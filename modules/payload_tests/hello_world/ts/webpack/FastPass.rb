class FastPass
  def self.url(key, secret, email, nickname, unique_id, secure=true, private_fields={}) ; end
  def self.script(key, secret, email, nickname, unique_id, secure=true, private_fields={}) ; end
end
